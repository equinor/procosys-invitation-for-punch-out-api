name: 🤖🦾 Run Tests

on:
  workflow_dispatch:
  pull_request:

permissions:
  id-token: write
  contents: read

jobs:
  run-test:
    name: Run tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        project: [
          Equinor.ProCoSys.IPO.Fam.Tests/,
          tests/Equinor.ProCoSys.IPO.Command.Tests/,
          tests/Equinor.ProCoSys.IPO.Domain.Tests/,
          tests/Equinor.ProCoSys.IPO.Infrastructure.Tests/,
          tests/Equinor.ProCoSys.IPO.MainApi.Tests/,
          tests/Equinor.ProCoSys.IPO.Query.Tests/,
          tests/Equinor.ProCoSys.IPO.Test.Common/,
          tests/Equinor.ProCoSys.IPO.WebApi.Tests/,
        ]
      fail-fast: false
    steps:
      - uses: actions/checkout@v4

      - name: 'Login to Azure'
        uses: azure/login@v2
        with:
          client-id: ${{ vars.EQUINOR_PROCOSYS_NON_PROD_DEVOPS_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          allow-no-subscriptions: true

      - name: Get DevOps Token
        id: 'get-devops-token'
        uses: ./.github/actions/get-devops-token
      
      - name: Configure Nuget
        uses: ./.github/actions/nuget-config
        with:
          access-token: ${{ steps.get-devops-token.outputs.token }}

      - name: Setup dotnet 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Run tests - ${{ matrix.project }}
        run: |
          dotnet test ./src/${{ matrix.project }} -c Release
